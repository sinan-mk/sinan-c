#include <stdio.h>
#include <stdio.h>

int main (void){
int i,limit=10,sinan;
      prinf("Enter a number");
    scanf("%d",&sinan);

    for(i=1;i<=limit;i++){
        printf("%d X %d=%d\n",i,sinan,i * sinan);
    }
}